describe('Top Level API', function() {
  it('exports a singleton Runtime instance as its default export')
  it(
    'exposes a function for creating instances of any Helper class in the context of the current runtime singleton'
  )
  it(
    'exposes the prop-types library for defining optionTypes, providerTypes, contextTypes on your Helper'
  )
  it('exposes the core Runtime, Helper, Feature, Registry classes')
})
